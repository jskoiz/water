# Water

New water-simulator project. Development is coordinated through the Sol Max orchestrator and split into Luna Max implementation lanes.

See [ORCHESTRATOR.md](./ORCHESTRATOR.md) for lane ownership, dispatch rules, and merge policy. The machine-readable configuration is in [.orchestration.json](./.orchestration.json).
